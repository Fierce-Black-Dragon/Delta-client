import React from "react";

const Input = (props: {
  type: string;
  name: string;
  handleChange: () => string;
  label: string;
  //   half;
  //   autoFocus;
  //   register;

  //   handleShowPassword;
}) => {
  return (
    <input
    //   name={name}
    //   onChange={handleChange}

    //   required

    //   label={label}
    //   autoFocus={autoFocus}
    //   type={type}
    />
  );
};

export default Input;
